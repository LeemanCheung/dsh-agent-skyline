/** Host-visible Node half for the Agent Skyline client plugin. */
export const name = 'agent-skyline'

/** All visualization and persistence run locally in the browser. */
export function apply() {}
